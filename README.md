# Wizards Only Fools - Unity Version

## Playable downloads

- [Download the Android APK](https://github.com/BDookie02/Wizards-Only-Fools-Unity-Version/releases/download/unity-version-v0.4.28/WizardsOnlyFools.apk)
- [Download the Windows version](https://github.com/BDookie02/Wizards-Only-Fools-Unity-Version/releases/download/unity-version-v0.4.28/WizardsOnlyFools-Windows-Unity-Version-v0.4.28.zip)
- [View the Unity Version release and testing notes](https://github.com/BDookie02/Wizards-Only-Fools-Unity-Version/releases/tag/unity-version-v0.4.28)

For Windows, extract the complete ZIP before launching `WizardsOnlyFools.exe`. On Android, download and install `WizardsOnlyFools.apk`.

This is the clean Unity recreation of the verified React/Node game at:

`D:\CodexProjects\Wizards-Only-Fools-React-Latest`

The React build remains the behavioral and visual oracle. The abandoned C++ rewrite is not part of this project. Work proceeds in small playable slices so a slice must compile, test, build, and run before the next parity slice is added.

The larger D-drive working snapshot is the current playable oracle; the clean GitHub `main` checkout at commit `0e293150cc9d92dcab19f8775889b3c43f2ee54a` is preserved separately as the public-history oracle. Their verified relationship and conflict rules are recorded in `Docs\Migration\REACT_SOURCE_PROVENANCE.md`.

`Docs\Migration\PARITY_MATRIX.md` is the acceptance ledger. It distinguishes implemented-but-unverified work from completed parity and prevents later slices from silently bypassing build, networking, mobile, or physical-interaction gates. The exact Android v0.4.28 artifact, preserved v0.4.17 runtime evidence, and current host-emulator limitation are recorded in `Docs\Migration\ANDROID_RELEASE_GATE.md`.

## Pinned production stack

- Unity `6000.3.21f1`, changeset `c02631ffc030`
- Universal Render Pipeline `17.3.0`
- Input System `1.20.0`
- Netcode for GameObjects `2.13.1` after an isolated `2.13.0` migration baseline
- Unity Transport `2.7.4`
- Unity Multiplayer Services `2.3.0`
- Unity Authentication `3.7.3`
- Unity Vivox `16.10.0`
- Unity Test Framework `1.6.0`
- CoplayDev Unity MCP `v10.1.0` over local `stdio`

The v0.4.28 interaction/presentation slice restores Peaceful Sanctuary as a second remote-dimension fast-travel option alongside Lily Coil. It routes through the existing Darrel Grove realm transition so the dojo, saved return position, and return gate remain intact; a final-player controller probe physically selected Base, Lily Coil, and Peaceful Sanctuary and captured the destination inside the dojo. All 26 spells now have stable equipped-hand presentation: 25 use their React-derived spell art or dedicated effect, while React's “Hands” spell intentionally uses the hand pose alone. Spell layers are always behind the hand so fingers and thumb occlude the spell, and the complete physical roster gate equipped and fired 26/26 spells without stale sprites or firing-layer reversals. Mobile survival lighting no longer adds React's ambient and hemisphere values together as HDR exposure; the final mobile-performance base frame retains terrain, wood, foliage, and hand contrast instead of washing to white. The complete 612/612 suite passes. The final Windows executable passed receipt/payload/`SESSION_READY` validation; its completely read 611-entry ZIP is 123,668,171 bytes with SHA-256 `0a3b368ec765862c071d2fda21cbb31dfc7c26b6d86abcfdcc149ddfe61f9c5f`. The 158,153,105-byte ARM64-only APK passes receipt, complete 1,389-entry/813,787,341-byte archive read, internal manifest, API/ABI, and v2-signature validation as `0.4.28`/code 31 with SHA-256 `c415a34e7d8ef39723963f303af88a6dce5009d1cb1df0c810abc0faeef49120`. No phone was attached for a fresh Android runtime frame, so representative-phone validation remains open.

The v0.4.27 presentation slice restores React's active custom-lobby sky as a separate mode instead of incorrectly reusing the survival day/night stack. Desktop custom lobbies now use the source's fixed 400 x 250 horizon at y 40 with 64 segments, no fog, no survival sun/moons/stars/clouds, exact classic lighting, and the Three `Sky`/Preetham atmosphere inputs `turbidity=0.3`, `rayleigh=0.5`, `mieCoefficient=0.005`, `mieDirectionalG=0.8`, and sun position `(50,20,50)`. Mobile custom lobbies retain React's flat `#9bdcff` background and mobile lighting rather than the desktop atmosphere. Solo and multiplayer survival retain the camera-following 2,816 x 2,200 horizon, fog, and full 600-second celestial cycle. Fresh 1,280 x 720 Windows custom-lobby, survival-day, and survival-night frames were physically inspected in the final player; hands, HUD, compass, terrain, trees, and the correct mode-specific sky layers remain intact. The complete 610/610 suite passes. The final Windows executable passed receipt/payload/`SESSION_READY` validation; its completely read 611-entry ZIP is 123,658,328 bytes with SHA-256 `f795d2481b8b520564265b4ff8a61adc57fdf09b28172268b232c53241a55a81`. The 158,147,801-byte ARM64-only APK passes receipt, complete 1,389-entry archive read, internal manifest, API/ABI, and v2-signature validation as `0.4.27`/code 30 with SHA-256 `8dca26c7dfe1e314c6645206cd8052fdc8024c276065599939102e3bc8eda7d5`.

The v0.4.26 world-quality slice ports React's active survival horizon instead of leaving the Unity distance sky blank. It reproduces the exact 2,048 x 1,024 three-layer seeded hill-and-tree texture, 2,816-unit camera-following open cylinder, 2,200-unit height, 330-unit center, 96 segments, day/dusk/night tint, and survival fog behavior. Fog is opt-in on the horizon material only, so the existing sun, eight moon phases, stars, clouds, astral overlay, terrain, and synchronized Breath-of-the-Wild-style grass tint retain their prior behavior. Fresh 1,280 x 720 day, night, and desert Windows frames were physically inspected, including a binary A/B against v0.4.25 and a rejected no-fog intermediate frame; hands, HUD, compass, trees, flowers, slopes, grass, sky sprites, and biome presentation remain intact. The complete 607/607 suite passes. The final Windows executable passed receipt/payload/`SESSION_READY` validation; its completely read 611-entry ZIP is 123,671,636 bytes with SHA-256 `18b1a0e0066cab5b04ef53181e2f7371b425cddcb396648eb9539060d406e5ec`. The 158,133,141-byte ARM64-only APK passes receipt, complete 1,389-entry archive read, internal manifest, API/ABI, and v2-signature validation as `0.4.26`/code 29 with SHA-256 `f6a273ff04e96a7d129bd69bcdb839fa0aba3bb91c350d43b5f740432d3e96af`.

The v0.4.25 world-quality slice fixes the custom Breath-of-the-Wild-style grass shader so the existing 600-second survival day/night cycle tints grass alongside terrain instead of leaving neon day-bright blades at night. The change is isolated to final grass color: the verified 56,000-blade/760-flower scatter, slope roots, wind, alpha, density, raised flowers, hands, HUD, and live compass remain unchanged. Fresh 1280 x 720 Windows day and night captures were physically inspected; day presentation is preserved, nighttime grass now follows the dark terrain and sky, and both complete in under four seconds. The complete 603/603 suite passes. The final Windows executable passed receipt/payload/`SESSION_READY` validation; its completely read 611-entry ZIP is 123,669,214 bytes with SHA-256 `bc522d677c7b525fb078bf68340daeb3bb9d236b061dc8ddd418a5c06e6baf68`. The 158,142,361-byte ARM64-only APK passes receipt, complete 1,389-entry archive read, internal manifest, API/ABI, and v2-signature validation as `0.4.25`/code 28 with SHA-256 `5a860ff54a3954f1d48c5e86e7ba12650a37b089f96e2f85302306417d4d013f`.

The v0.4.24 world-quality slice ports React's deterministic survival waterfalls without inventing animation or particle behavior absent from the source. The current near wilderness chunk receives at most one exact source record after React's biome/hash, authored-location, restored-meadow, terrain-height, water-level, and drop gates. The Unity runtime reproduces the source's two static transparent vertical planes and 18-segment pool, exact transforms and colors, no colliders, and current-chunk-only lifetime. The complete 602/602 suite passes, including exact React oracle records and the runtime mesh/material contract. The final Windows executable passed receipt/payload/`SESSION_READY` validation and a physically inspected 1280 x 720 jungle waterfall probe with the original hands, HUD, compass, terrain, trees, grass, and pool intact. The completely read 611-entry Windows ZIP is 123,673,961 bytes with SHA-256 `9398c5724800d71f7a847354f70cea756c85dfc36ab737d7a0eaf5e81a4d4b84`. The 158,127,201-byte ARM64-only APK passes receipt, complete 1,389-entry archive read, internal manifest, API/ABI, and v2-signature validation as `0.4.24`/code 27 with SHA-256 `7f285496e55882f46189dfd0ac65ff91275746de58d849da70e2c7aafc55807e`.

The v0.4.23 world-quality slice ports React's deterministic streamed-desert landmarks instead of inventing desert substitutes. Near desert chunks receive exact pyramid or obelisk records through React's source hashes, two-attempt-radius counts, route/water/footprint/type gates, continuous source-terrain height sampling, exact position/yaw/scale/variant values, stage-two distance/jitter timing, one-ready-chunk-per-frame construction, and retained 37-chunk readiness timers. Pyramids reproduce the stepped adobe shell, open three-tier entry, ramp, colliders, and three interactive Dune villagers; obelisks reproduce the shaft, cap, base, and flanking pillars. The complete 599/599 suite passes. The final Windows executable passed receipt/payload/`SESSION_READY` validation and physically inspected 1280 x 720 pyramid and obelisk probes with the landmarks, original hands, HUD, compass, desert terrain, trees, cactus, and scatter intact. The completely read 611-entry Windows ZIP is 123,660,463 bytes with SHA-256 `d2b3617f9cb51a26f6bbfb66112ea98cae0ba95b2e384a30981495b433f430f0`. The 158,095,881-byte ARM64-only APK passes receipt, complete 1,389-entry archive read, internal manifest, API/ABI, and v2-signature validation as `0.4.23`/code 26 with SHA-256 `86ca0d5b1d229996c6ad0b34510f721db59a3a33c0cbd74d13cb229782c5e3e3`.

The v0.4.22 world-quality slice ports React's deterministic survival hobbit huts instead of substituting generic houses. Plains, jungle, and mushroom chunks use the source hashes, biome thresholds, ten-candidate search, route/terrain/normal/height-range/water/cardinal rejection, exact yaw/scale/variant values, living earth-and-leaf roofs, timber façades, glowing doors, chimney smoke, biome palettes, single compound collider, mobile/inspection exclusions, 3.6-second stage-five distance/jitter delay, and retained 37-chunk readiness timers. The complete 595/595 suite passes. The final Windows executable passed receipt/payload/`SESSION_READY` validation and physically inspected 1280 x 720 plains, jungle, and mushroom probes with the huts, smoke, hands, HUD, compass, terrain, grass, trees, and flowers intact. The 158,037,633-byte ARM64-only APK passes receipt, complete 1,389-entry archive read, internal manifest, API/ABI, and v2-signature validation as `0.4.22`/code 25.

The v0.4.21 world-quality slice ports React's late-stage deterministic detail scatter instead of filling the world with substitute props. Ordinary wilderness chunks receive four source-derived hero trees, jungle five, swamp four, mushroom three, and desert nine exact prop decisions split between tumbleweeds and the deliberately suppressed old React cactus entries. Placement uses React's hashes, route clearance, rendered-terrain sampling, surface-normal/height-range/water rejection, biome scale formulas, rotations, low-poly branch/canopy/vine geometry, tumbleweed rods and dodecahedral wire form, 3.6-second stage-five delay, distance/jitter timing, and retained 37-chunk readiness timers. The approved 30-cactus thick saguaro runtime remains the only rendered desert cactus layer. The complete 591/591 suite passes. The final Windows executable passed receipt/payload/launch validation and physically inspected 1280 x 720 source-tree and tumbleweed probes with the original hands, HUD, compass, terrain, flowers, surrounding foliage, and thick cactus intact. The 157,962,981-byte ARM64-only APK passes receipt, complete archive, internal manifest, ABI, and v2-signature validation as `0.4.21`/code 24.

The v0.4.20 world-quality slice ports React's exact deterministic survival underbrush instead of adding substitute foliage. Bush clusters and fern fronds use the source hashes, rendered-terrain sampling, normal/range/water/route rejection, near/mid/mobile densities, biome palettes, positions, rotations, scales, 0.88 fern opacity, and staged tree-load timing. Bushes use an underbrush-only unlit shader with React's true 108-vertex non-indexed barycentric facet lines; ferns retain the source's transparent double-sided plane form. The runtime preserves ready overlapping chunks across 3 x 3 window moves, carries partially elapsed readiness timers forward, and stages only the entering fringe, preventing whole-window pop-out at chunk boundaries. The complete 588/588 suite passes. The Windows player passed receipt/payload/launch validation and physically inspected 1280 x 720 bush and fern probes with the hands, HUD, compass, terrain, trees, and flowers intact. The ARM64 APK passes internal manifest, archive, ABI, and v2-signature validation as `0.4.20`/code 23.

The v0.4.19 world-quality slice ports React's exact deterministic survival rock outcrops instead of inventing replacement scenery. Near wilderness chunks receive four outcrops, jungle receives five, the mid contract retains one, and desert, authored-village, restored-meadow, tallgrass, water-suppressed, and far contracts retain the source exclusions. Exact React hashes control placement, rendered-terrain height, normal/range rejection, scale, yaw, boulder versus five-sided spire form, and default/swamp palettes. Runtime work stays inside the player-centered 3 x 3 near window, follows the exact staged readiness delay, generates at most one newly ready chunk per frame, and renders instanced palette/form batches. The complete 584/584 suite passes. The Windows player passed receipt/payload/launch validation and a physically inspected 1280 x 720 exact-sample spire probe with surrounding hands, HUD, compass, terrain, flowers, and trees intact. The matching ARM64 Android artifact passes internal manifest, archive, ABI, and v2-signature validation.

The v0.4.18 world-quality slice adds React's exact six deterministic massive world willows, including biome-specific low-poly bark/canopies, eight branches, eleven canopy lobes, fourteen hanging vines with leaves, and 72 desktop/36 mobile falling glow particles. Their React render radius, particle radius, village avoidance, terrain height, yaw, scale, biome, grass-inspection suppression, and 24 Hz mobile update rules are locked by tests. The final Windows player physically passed a two-frame 1280 x 720 probe with visible particle motion and surrounding terrain, foliage, HUD, hands, and minimap intact. Its complete automated gate was 581/581 tests; the v0.4.19 count above supersedes older historical counts in the long-form project narrative below.

The first recreation slice contains one launch flow, first-person movement and camera input, native Unity Input System controller support, desktop and mobile controls, dual-hand fireball casting, server-authoritative health/armor/death/respawn, WebSocket-compatible transport, and a two-process LAN combat probe. The second in-progress slice ports the base village's deterministic 512 x 512 terrain, road/moat bands, 307-hut/45-path layout, four hut families, compound colliders, 480-unit arena walls, satellite trees, tree-house village, exact Three.js-baked faceted bushes, water and movement ripples, and the React campfire's low-poly visual, flicker, and fractional server-authoritative damage into reproducible scene generation. It also bakes all 307 deterministic React villagers through the canonical canvas avatar renderer into 52-frame compact archives, preserves idle/blink/startled/angry/facing/jump behavior, reproduces the procedural square-wave yelp, and resolves villager facing against the same nearest alive local-or-remote player rules as React. Darrel now uses his exact special 52-frame avatar archive and React targeting/dialogue rules, including the healing-crystals quest branch, content-sized 760 px desktop modal, profile persistence, keyboard/mouse input, and native controller X/D-pad/A/B interaction. The exact Darrel ingredient, brew, drink, dragon-fight, dragon-peace, fatal-damage, spell-unlock, one-crystal return-gate completion, and two-crystal peaceful reward transitions are also ported. Darrel's source-authored sacred garden is generated at its exact React world chunk with the original terrain, hut, moat, bridges, stairs, river, animated waterfall/runnels/spray, bonsai layouts, petals, Fuji backdrop, return gate, and all 44 original Spirit Dragon frames. The React quest-navigation chain is reproduced with its exact source positions, labels, colors, pulsing beam/rings, and 64 x 64 canvas icon for fields, brewing, drinking, Spirit Dragon, and Darrel turn-in states. The React command-console shell is reproduced at its exact 720 px desktop width with the complete ordered suggestion/filter catalog, 90-character input, physical Slash/Enter/Escape behavior, mouse suggestion selection, `/inventory`, `/forage`, `/brew`, and the now-active `/drink` command, which consumes the draught only when the server-authoritative garden teleport is accepted. Generic villagers use the exact persistent random spell assignment, first/repeat/ready/completed message rules, five-message/12-second React notification feed, physical F input, and native controller X input. The desktop gameplay overlay reproduces the bottom-anchored GRIMOIRE/SPELLS/VITALITY/AETHER status bar and dual mana meter. The React survival inventory and quest journal are ported with the exact 27-slot backpack, 9-slot quick row, five item definitions, active-quest/status rules, Darrel progress rows, screen-pixel desktop sizing, physical I/J/Enter/Escape/arrow input, and native D-pad Right/A/B/Start navigation.

The authored survival world covers React's complete 11 x 8 atlas footprint: 82 connected generated terrain chunks plus the six separately authored Base, Chicago, Swamp, Desert, Mountain, and Graveyard chunks. Outside that atlas, an exact player-centered 37-chunk runtime window now reproduces React's 512-unit coordinate rules, 368.64-unit recenter hysteresis, 32/12/4 render LODs, radius-two 32-segment collision meshes, biome/height/color/river formulas, skirts, near/mid tree groves, near/mid river/pond/lily surfaces, and Lily/authored-location exclusions. Physical Windows probes at `(7,4)` and `(-17,9)` verified continuous terrain, colliders, player-centered grass, trees, water, and the existing pixel treatment with no runtime exceptions. React routes are terrain and vegetation masks rather than standalone rendered props, and those masks already apply to every streamed chunk. The desert village at chunk `(4,-4)`, Chicago at chunk `(-3,-3)`, swamp village at chunk `(0,-3)`, and mountain village at chunk `(3,0)` retain their authored layouts and behavior. The desert keeps its exact 17.885722662941443 base height, 55 buildings/villagers, 52 walls, 10 markets, 22 palms, 37 ladders, 41 fences, 15 clotheslines, 94 street props, source road/pad meshes, procedural sand/adobe textures, collision structures, and 55 original 52-frame villager archives. Chicago keeps its exact 21.912045982731858 base height, 35 enterable buildings/operators, four landmarks, 46 animated vehicles, 220 animated pedestrians, intersections, street furniture, Cloud Gate, source-generated facade/sign/ad textures, and original operator sprites in a separately streamed Unity scene. The swamp keeps its exact 2.7529895363497836 base height, water/platform levels, 13 stilt huts/villagers, 17 walkways, four ramps, 28 lily pads, 18 stumps, 36 reed patches, 91 rope segments, 39 bulbs, original 28-frame idle/12-frame yawn/sleep toad animation, exact colliders, and 13 original 52-frame villager archives in its own streamed scene. The mountain keeps its exact 3.364967894227928 base height and 217.54496789422794 protected summit center, source terrain/trail/summit meshes, 1,793 emitted slope-grass tufts, 48 cliff patches, eight summit cabins, three mineshaft huts, four native-controller ladders, waterfall, opening/catwalk/wall/banquet detail, and 11 original 52-frame villager archives in a fourth streamed scene. Its Unity-only terrain perimeter now forms an irregular caldera rim and broad 500-unit shoulder while preserving every central structure position. Quest targeting resolves across all village managers while preserving the base manager's exact 307-NPC contract and exact desert/swamp/mountain town identities.

The current world-quality work adds player-centered, instanced Breath-of-the-Wild-style pixel grass with up to 56,000 tufts and 760 raised flowers. Each tuft uses three compact, root-offset alpha-cutout layers sampled from the exact React grass texture, with a muted root-to-tip gradient and the source alpha cutoff. A deterministic 32-bit hash scatter replaces the former golden-angle/prime-permutation rows so overhead and sloped views no longer form repeating brushed waves. Smoothed terrain normals keep every root tangent to slopes while upper growth remains gravity-upright. The verified ground and true 26-metre overhead release views emit all 56,000 tufts and 760 flowers, complete the incremental field in 3.43-3.46 seconds with a 9.59-11.08 ms observed build-slice range, and keep the raised flowers visible. The exact dense React tree formulas supply 2,526 biome trees across 24 source meshes inside the atlas and deterministic near/mid groves beyond it. The exact 600-second React day/night cycle retains its procedural sun, eight moon phases, clouds, stars, and terrain tint, and the 0.46 point-upscaled world render restores React's subtle pixel treatment without lowering HUD resolution. React's mana ecology now includes collectible wilderness flowers, the infinite base source at `(11.5, 31.5)`, the desert well's 34-metre source radius, rotating hut runes with exactly two thirds active per 15-second cycle, and the three-ring pickup pulse; every refill is server-authoritative and targets the most-empty hand bar. The compact compass is an unclipped live local map with its own restrained color grade and tiny purple waypoint wizard hat outlined in red for visibility, while D-pad Left/keyboard M/touch opens the original 4096 x 2979 world atlas with controller and touch waypoint controls, a live player marker, highlighted current region, and persistent exploration reveal across the complete 11 x 8 world grid. Server-authoritative fast travel is available to Base, Chicago, Swamp, Desert, Mountain, Graveyard, Lily Coil, and Peaceful Sanctuary. The spell book exposes all 26 React spells at once at desktop and phone-landscape sizes, preserves the original mouse/trigger casting scheme, and uses the outward equipped-hand pose with its four-frame 280 ms idle breathing loop and a separate 140 ms same-pose firing flex. Lily Coil has source-derived tunnel, eye animation, grass, lilies, flowers, fireflies, butterflies, seals, and exterior/inside release captures instead of placeholder geometry. A live normal-Chrome comparison at React's exact spawn confirms the current Unity interior and also confirms that the source has no Lily Coil NPC, quest giver, dialog, or interaction object: its quest-related name is only the spawn/teleport identifier. Unity retains all five React teleport aliases without inventing content. A built-player native-gamepad probe now traverses Lily Coil from the exact lower cap to `t=0.9952` through the production `CharacterController`: 4,712.08 metres walked in 76.92 seconds, 100% sprinting and grounded frames, zero backtracking, and zero surface error. The mountain perimeter now uses dirt, stone, and snow elevation bands with village snowfall, and the disconnected spiral is replaced by a narrow, surface-sampled south-face switchback with a continuous walkable collider. A built-player native-gamepad probe traverses all 98 route points from trailhead to summit through the real `CharacterController`: 1,034.67 metres walked in 81.01 seconds, 0.89-metre maximum cross-track error, 83.2% grounded frames, and 0.50-metre final error. The command console now executes React's day/night overrides, server-authoritative VCLIP with the original Space/C flight controls, 125 ms navigation recording/JSON export, `/engine`, direct `/place`, the complete `/questdev`/`npcdev`/`devquests` editor family, and all Darrel claim/spawn/reset aliases. The runtime quest-NPC editor reproduces React's roles, 24-point script list, reorder/copy/delete/reset controls, seven event presets, custom event builder, preview/test execution, right-click villager targeting, full command opening, D-hosted atomic program storage, and overridable Darrel realm spawn while preserving the authored fallback. The engine menu ports the exact ordered 12-object React catalog with categories/search, grid and yaw controls, five-point terrain/slope/collision validation, preview/move/delete/clear actions, six D-hosted save slots, and multiplayer-replicated records; its training dummy is replicated for the current session but deliberately excluded from editor counts and persistence, matching the source behavior. The dummy now also uses the exact React 120-health, spell-specific-damage, knockdown, 1.8-second respawn, and world health-display behavior under server authority. A deterministic two-process gate now proves that a remote client can own and place the dummy, acknowledge its replicated initial state, receive all five exact Fireball health transitions, collapse at zero, and receive the timed 120-health respawn from the server. Survival profiles now migrate schema 1 to schema 2, retain the previous valid generation during durable replacement, quarantine corrupt primaries, recover missing or corrupt primaries from backup, keep device settings separate, and autosave at the exact React 15-second cadence plus application pause/quit. The current automated gate is 612/612 tests over 1,266 baked React outputs plus independently hashed bootstrap/Chicago/swamp/mountain/graveyard/Lily-Coil scene payloads, exact React-derived terrain/tree/water/foliage/hobbit-hut/desert-landmark/survival-waterfall streaming fixtures, Windows executable/additive-scene launch, a live Unity Relay host/client join with distinct authenticated profiles, Vivox `CONNECTED` on both public peers, visible executable quest-editor interaction at 1280 x 720 and 1024 x 600, a visible executable corrupt-save recovery/migration/autosave probe, warmed out-of-atlas runtime probes, 26/26 physical spell-menu equip/cast coverage, live-map desktop/Android interaction, physical command-console/sky/VCLIP/navigation/engine-placeable/training-dummy-combat/pause/hold-roster/four-frame-hand coverage, built-player native-controller engine-menu category/select/place/back coverage, base/desert/swamp/mountain-generic-quest/Darrel-dialog/Spirit-Dragon controller probes, mountain left-stick ladder and complete trail-traversal probes, the production Lily Coil cap-to-cap native-gamepad probe, the production-path Darrel drink/garden/return/save probe, client-owned training-dummy LAN authority/replication, villager/audio smoke, and release-player captures. The Android 15 emulator install/landscape/touch-Solo/spell-book/pause/map/waypoint/Lily-Coil/controller-visibility and Android-to-Windows Relay gates now pass. Real-phone pinch/performance/lifecycle/Relay, audible two-device microphone behavior, additional Lily Coil exterior source viewpoints, two-peer visual interaction, and awake physical-controller testing remain open. `Docs\Migration` contains the broader parity inventory and the still-open physical/visual gates.

Current native controller bindings mirror the React defaults: left stick moves and climbs mountain ladders, right stick looks, A jumps/selects and drives the React jump thruster while held, X interacts, L3 latches sprint while movement continues, B holds slide/crouch in gameplay and goes back/closes in menus, LT casts the left hand, RT casts the right hand, Start submits highlighted actions or backs out of menus, and D-pad/left stick navigate menus and dialogue choices. D-pad Up opens the spell book, D-pad Left opens the live map, and D-pad Right retains the React inventory shortcut exactly: while standing still, a release after a tap shorter than three seconds opens inventory; A opens its journal and B/Start backs out. Mobile touch controls automatically disappear whenever Unity reports a connected controller. Sprint, slide, crouch, thruster, and ladder traversal use the React speeds, timing, camera-height delta, and original baked directional animation frames. The runtime reports every controller Unity recognizes, including hot-plug changes. Automated native-device integration and Windows-player inventory, spell-menu, live-map, gate-traversal, thruster, base-villager, exact desert/swamp/mountain villager/town, exact mountain ladder-climb and trailhead-to-summit traversal, Darrel-dialog, and two-outcome Spirit-Dragon probes pass; the dragon probe verifies the actual in-world controller prompt, X interaction, both LT/RT proximity interactions, controller navigation, fatal server damage, death, respawn, peaceful completion, reward, and close. The command console retains React's keyboard-only Slash opening rather than inventing a controller shortcut, while native Gamepad movement, look, jump, sprint, slide, and casting are regression-tested as suppressed whenever the console owns input. A physical controller must still be awake and reported by Unity before hardware interaction can be marked complete.

The Android v0.4.28 artifact passes 612/612 tests and internal package/signature validation: ARM64-only, package `com.WizardsOnlyFools.WizardsOnlyFools`, version `0.4.28`/code 31, API 25/36, and APK Signature Scheme v2. Its completely read 1,389-entry/813,787,341-byte, 158,153,105-byte APK has SHA-256 `c415a34e7d8ef39723963f303af88a6dce5009d1cb1df0c810abc0faeef49120`. A new in-player Android base-lighting capture is not claimed because no phone was attached over ADB and the local QEMU host still crashes with Windows exception `0xc0000005` during Unity startup; the exact published v0.4.17 control APK produces the same host crash on a cold-wiped AVD, so the evidence does not identify a v0.4.28 gameplay regression. The exact mobile-performance lighting branch was physically inspected in the final Windows player, while the preserved v0.4.17 gate remains the latest successful Android runtime evidence. Real-phone lighting, pinch/performance/lifecycle/Relay, physical-controller, and audible microphone/Vivox remain open.

## One-command automation

Inspect the complete resumable pipeline without launching Unity, installers, or MCP:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File 'D:\CodexProjects\Wizards-Only-Fools-Unity\Tools\automate-unity-recreation.ps1' plan
```

Provision and resume every automatable stage:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File 'D:\CodexProjects\Wizards-Only-Fools-Unity\Tools\automate-unity-recreation.ps1' resume -Apply
```

That command is idempotent and stage-aware. It verifies or installs the signed Editor, Unity Hub, and platform modules, installs the pinned D-hosted Unity MCP runtime, performs the two-phase package migration, refreshes the existing Hub entitlement immediately before Unity work, compiles and tests the project, builds Windows/WebGL/Android, and runs the two-process LAN authority probe. Checkpoints bind output evidence to deterministic fingerprints of Unity source, tests, art inputs, packages, settings, and runner scripts, so a source edit cannot inherit a stale green build.

Validate existing platform artifacts without launching the Unity Editor or rebuilding. Windows validation also starts the built player headlessly and requires a real `SESSION_READY` marker so a corrupt scene payload cannot pass on hashes alone:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File 'D:\CodexProjects\Wizards-Only-Fools-Unity\Tools\wof-unity.ps1' verify
```

The intentionally expensive lower-level full rebuild is named `rebuild-all`; it is not invoked by `verify`.

Two security boundaries intentionally remain manual:

1. Approve the signed Unity platform component installers if Windows displays UAC. Verified reruns do not request elevation again.
2. If Unity reports no license, run the following command, sign in and activate Unity Personal through Unity Hub in the normal signed-in browser, then rerun `resume -Apply`:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File 'D:\CodexProjects\Wizards-Only-Fools-Unity\Tools\automate-unity-recreation.ps1' open-activation -Apply
```

Automation state is recorded atomically at `D:\UnityAutomationState\Wizards-Only-Fools-Unity\automation-status.json`, so a stopped run reports its exact stage and safe next action.

## D-drive boundary

Project sources, Unity editors, Unity Hub binaries and Electron data, installers, modules, package caches, Android/Gradle state, MCP runtime, Python, temporary files, logs, backups, automation state, and build outputs are pinned to `D:`. The normal browser account profile is reused instead of creating an alternate Chrome identity. Windows, the Codex host, certificate validation, the tiny `unityhub://` protocol registry entry, and Unity licensing services can still use existing operating-system-managed state on the system drive; project scripts cannot safely relocate or bypass those facilities.

After automated verification passes, physical interaction at relevant desktop and mobile/WebGL sizes is still required before a parity slice is declared complete.
